server:
  port: 8081                  # 소매(8080)와 나란히 띄운다

spring:
  application:
    name: wholesale-api
  profiles:
    default: local
  jpa:
    hibernate:
      ddl-auto: validate      # 스키마는 마이그레이션이 만든다. 엔티티와 어긋나면 앱이 안 뜬다
    open-in-view: false
