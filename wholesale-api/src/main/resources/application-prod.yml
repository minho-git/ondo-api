# 운영 환경 (MUL-104). 아직 안 띄웠다 — 지금 배포된 건 dev 다.
#
# 배포 공통은 application-deploy.yml 에 있다. 여기엔 개발 환경과 **달라지는 것**만 둔다.
#
# 지금은 비어 있다. 도매는 개발 환경에서 푼 게 시드뿐이고, 그건 dev 파일에만 있어서
# 여기서 되돌릴 것이 없다. 소매와 달리 쿠키를 푼 적도 없다 — 도매 화면은 배포된 게 없다.
#
# 늘어날 자리 —
#   · 문서(springdoc) 닫기 · 시드 계정 차단 (MUL-103)
#   · CORS 허용 주소 (도매 화면이 뜨면. 지금 도매엔 CORS 설정 자체가 없다)
#   · 로그 레벨
#
# 파일을 비워두고 남기는 이유 — 없으면 다음 사람이 운영 설정을 어디에 적을지 몰라
# application-deploy.yml 에 적는다. 그러면 개발 환경까지 같이 바뀐다.
{}
