# 로컬 개발용. db/compose.yml 로 띄운 도매 PostgreSQL 을 본다.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ondo_wholesale
    username: ondo
    password: ondo

# 로컬 프론트 dev 서버(리액트, 3000)의 교차 출처 호출 허용 (MUL-86)
ondo:
  cors:
    allowed-origins: http://localhost:3000
