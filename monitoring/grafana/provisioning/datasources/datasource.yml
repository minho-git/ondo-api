# 그라파나가 뜰 때 프로메테우스를 데이터소스로 자동 등록한다 (MUL-115).
# 컨테이너끼리는 서비스 이름(prometheus)으로 통신한다.

apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus       # 대시보드 JSON 이 이 uid 로 참조한다
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
